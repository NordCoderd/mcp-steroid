# 0.87.0

- Previous tag: none
- Commit range: `a874f57147c8c258e3bd84c96a7d99ce4e9a0108..HEAD`

## Features
- Added automatic modal dialog handling around `steroid_execute_code`, including a `dialog_killer` parameter override and default-on behavior to reduce blocked runs (`62b3800`, `aec0dd1`, `b21a9f5`, `7bd8774`, `23f27f0`).
- Added multi-IDE product support in release/build flows with PyCharm integration test coverage (`4272344`, `4465eec`).
- Added configurable kotlinc settings through registry keys (`mcp.steroid.kotlinc.parameters`, `mcp.steroid.kotlinc.home`) with safer parameter parsing for version-mismatch workarounds (`7421705`, `891ccb1`).
- Improved agent console/output processing for integration runs, including NDJSON parsing with `kotlinx.serialization`, stream filtering, and progress-friendly output (`5158f79`, `c7f4696`, `521787a`, `66140ce`, `5fd1b8f`).
- Expanded MCP/testing support for debugger and execution scenarios, including direct MCP execution helpers and additional debugger resources (`8816388`, `4bfeeaf`).

## Fixes
- Fixed EDT/modality/threading issues in dialog handling and vision flows, improving stability when modal dialogs are present (`721b7ec`, `1d89683`, `a99d2c1`).
- Fixed resource-leak and action-context issues in prompts/execution paths (`efa8263`, `d61b13d`, `04d5790`, `f72536a`).
- Fixed screenshot collision and OCR native-loading issues in automation/test environments (`fb14acc`, `730814c`).
- Fixed debugger/integration reliability issues, including expression-evaluation race conditions and startup/layout ordering problems (`679ca17`, `1ceef7c`, `5fe49c5`, `8f318ec`).
- Fixed Docker/test-run lifecycle issues, including container reaper connectivity, test-run directory reuse, and same-port MCP test restarts (`1db23f0`, `1618fbe`, `bdc15fa`, `8dcbe46`).

## Infra/Build/Test
- Reworked container cleanup infrastructure from Ryuk-based behavior to a dedicated Docker reaper flow with stronger cleanup/test coverage (`42189a2`, `b4d19bf`, `1618fbe`, `486c788`, `14e879d`).
- Hardened test execution defaults and build stability (larger test heap, project-home wiring, download retries/timeouts, compatibility checks) (`ec133c7`, `146ed89`, `1e68d63`, `4d9fbb8`, `25e5c78`).
- Improved integration-test reliability and observability for CLI agents, console rendering, UTF-8 output, and timeout handling (`e531822`, `b7650b7`, `72592c3`, `f120d80`, `da1ead8`).
- Improved release automation robustness: local-range notes generation, macOS temp-file handling, dry-run/publish safeguards, and API-key forwarding in builder containers (`e7f947d`, `e2d6a6a`, `2764acb`, `e122c07`, `2b50567`, `684a147`).

## Docs
- Added dialog-killer documentation in config and website docs (`9882bd2`, `6d26b6b`).
- Updated debugger and integration docs to reflect current evaluator/EDT APIs and test infrastructure (`02a0f90`, `9d0dfe4`, `b537c95`).
- Updated release workspace documentation and TODO tracking for completed/stabilization work (`bed5d2d`, `165d206`, `d9c4712`, `75ee752`).
