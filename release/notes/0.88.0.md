# 0.88.0

- Previous tag: `0.87.0`
- Commit range: `0.87.0..HEAD`

## Features
- Added support for IntelliJ 2026.1 EAP IDE versions (`69883f6`).
- Updated bundled Kotlin compiler to 2.3.10 (`18d6635`).
- Added integration tests for IntelliJ IDEA, PyCharm, GoLand, and WebStorm (`1d3f8d8`, `0305f12`).
- Added dpaia.dev arena test infrastructure for integration tests (`3abe453`).
- Added MCP resources for debugger expression evaluation, step-over, and run configs (`4bfeeaf`).
- Added project home directory resolution and MCP resource URI helpers (`626e279`).

## Fixes
- Fixed dialog killer exception handling and EDT threading issues; improved integration tests (`1298431`, `68a5041`, `474fb65`, `721b7ec`).
- Fixed Docker reaper multi-endpoint connectivity for builder containers (`1db23f0`).
- Fixed MCP server test restart on same port with configurable test port (`8dcbe46`).
- Fixed OCR native library loading with macOS dylib alias support and lept4j version pinning (`730814c`, `cac6b42`).
- Fixed screenshot filename collisions with unique subdirectories (`fb14acc`).
- Fixed critical resource leaks in coroutine lifecycle management (`f72536a`).
- Fixed "Collecting data..." race in debugger expression evaluation (`679ca17`).
- Fixed plugin unzip to overwrite existing files, enabling test run directory reuse (`bdc15fa`).
- Fixed executable file permissions and assertions (`e19e157`, `b8b8c0c`, `ba9768e`).

## Infra/Build/Test
- Centralized plugin versions and downgraded Kotlin to match IDE; added Kotlin plugin version assertion to compatibility check (`696feec`, `eb13f14`).
- Updated IntelliJ SDK version (`916194e`).
- Set buildSrc jvmToolchain to Java 21 (`670f7f1`).
- Bumped Kotlin to 2.3.10 in subproject build files (`18d6635`, `696feec`).
- Improved release builder with EAP resolution and stage exclusions (`6faa294`, `26f585e`).
- Switched DockerReaper to filesystem paths, removed classpath resources; added reaper Docker context (`73ffbaf`, `812fc43`).
- Hardened CLI integration tests for Gemini output and transient API failures (`4e5b039`).
- Rewrote Codex CLI session reset and fixed project_name prompts (`3f5ed2b`).
- Added DialogKiller unit tests and updated integration tests (`b5eab34`, `486b69d`).
- Added polling fallback for complex object presentation after step-over (`34e86d5`).
- Refactored test infrastructure: extracted LayoutManager, GitDriver, ConsoleDriver (`aee479e`, `52df522`, `b7650b7`).
- Added three new debugger integration tests with improved evidence validation (`336afe0`).
- Enforced no hardcoded MCP resource URIs in Kotlin sources (`df532d3`).
- Propagated version to subprojects (`b322369`).
- Optimized CLAUDE.md from 1099 to 234 lines (`2622bc1`).

## Docs
- Added ModalityState.any() guidance to coding-with-intellij prompt (`e58ad95`).
- Added Junie agent trajectory analysis findings (`87fcef7`).
- Tracked GoLand/WebStorm release matrix and smoke test tasks (`e994e2a`).
- Updated EDT-related TODOs (`7c86d28`).
- Added Docker integration test documentation to CLAUDE.md (`b537c95`).
- Updated debugger and MESSAGE-BUS docs with test progress (`ee80db1`).
