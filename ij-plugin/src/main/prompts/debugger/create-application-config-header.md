Create Application Run Configuration

Create a new Application run configuration for a Kotlin/Java main class.
Use this before starting a debug session when no run configuration exists.

IntelliJ APIs: RunManager, ApplicationConfiguration, ApplicationConfigurationType
